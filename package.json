{
  "name": "ratio-spoof-manager",
  "version": "2.0.0",
  "description": "Modern GUI for ratio-spoof built with Tauri",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "engine:fetch": "node scripts/fetch-engine.mjs",
    "engine:check": "node scripts/fetch-engine.mjs --check",
    "engine:update-lock": "node scripts/fetch-engine.mjs --update-lock"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "@tauri-apps/plugin-fs": "^2.0.0",
    "@tauri-apps/plugin-notification": "^2.0.0"
  }
}
